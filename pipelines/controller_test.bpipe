// A dummy pipeline to tsest the job controller

about title: "This pipeline simply prints the name of a set of files into an output."

inputs "fastq.gz" : "Input must be a gzipped fastq file (fastq.gz)"

// load 'pipeline.config'


run { "%-%_%_R001.fastq.gz" * [ print_file_name ].using(paired:true)  }


